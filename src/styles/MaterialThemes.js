import { createTheme } from '@mui/material';

export const hitori = createTheme({
  palette: {
    primary: {
      main: '#FAAABC',
    },
  },
});

export const nijika = createTheme({
  palette: {
    primary: {
      main: '#F2D677',
    },
  },
});
export const ryo = createTheme({
  palette: {
    primary: {
      main: '#4C6CA5',
    },
  },
});
export const ikuyo = createTheme({
  palette: {
    primary: {
      main: '#D45850',
    },
  },
});

export const themeSelect = {
  hitori,
  nijika,
  ryo,
  ikuyo,
};
